{# New meet #}
{% extends "base.html.j2" %}
{% block content %}
  <h1 class="mb-5">Create New Meet</h1>
  <form action="" method="POST">
    <div class="row">
      <div class="card p-4 col-lg-6 mb-4">
        <h4>Meet Information</h4>
        <div class="form-group">
          <label for="meetName">Meet Name</label>
          <input type="text" class="form-control" id="meetName" placeholder="Enter name of meet" name="name">
        </div>
        <div class="form-group">
          <label for="meetDate">Meet Date</label>
          <input class="form-control" id="datepicker" width="200" name="date" placeholder="MM/DD/YYYY" />
        </div>
        <div class="form-group">
          <label for="meetDescription">Description</label>
          <textarea class="form-control" id="meetDescription" name="description" rows="10" placeholder="Tell us the important information that teams entering your meet need to know, such as location, start time, divisions, events, and any special rules.">
          </textarea>
        </div>
        <div class="form-group">
          <label form="meetHost">Host School</label>
          <select class="form-control" id="datepicker" width="200" name="date" disabled>
            <option>{{session['user_school_name']}}</option>
          </select>
        </div>
      </div>
      <div class="card p-4 col-lg-6 mb-4">
        <h4>Meet Rules</h4>
        <div class="form-group">
          <label for="maxEntries">Max number of events per athlete</label>
          <input type="text" class="form-control" id="meetName" placeholder="4" name="max_entries_per_athlete">
        </div>
        <div class="form-group">
          <label for="maxTeamEntries">
            Max athletes per team allowed in each event
          </label>
          <input type="text" class="form-control" id="maxTeamEntries" placeholder="3" name="max_teammates_per_event" aria-describedby="athletesPerTeamHelp">
          <small id="ahletesPerTeamHelp" class="form-text text-muted">
          By "event", we really mean "in each division within an event". So, enter
          3 if you allow 3 kids from the same team in the same division to compete in the 100m.
          </small>
        </div>
      </div> #card
    </div>
    <div class="d-flex justify content-center">
    <button type="submit" class="btn btn-primary col-md-4 ">Submit</button>
  </div>
  </form>
{% endblock %}

{% block my_javascripts %}
  {# <script type="text/javascript" src="static/js/tms-datatable.js"></script> #}
  <script src="https://cdn.jsdelivr.net/npm/gijgo@1.9.10/js/gijgo.min.js" type="text/javascript"></script>
  <script>
  $('#datepicker').datepicker({
  uiLibrary: 'bootstrap4'
  });
  </script>
{% endblock %}